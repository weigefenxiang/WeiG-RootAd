> **Testing build / 测试版本** — manager `{{VERSION}}`, workflow run `#{{RUN_NUMBER}}`.
>
> This rolling pre-release is replaced after every push to `main`. It is intended for testing before a stable release.
>
> 此滚动预发布版本会在每次推送到 `main` 后更新，仅用于正式发布前测试。

**Most users / 普通用户：** download `WeiG-RootAd-test.zip` and install it with MMRL, Magisk, KernelSU, or APatch. It includes both the core and manager. / 下载 `WeiG-RootAd-test.zip`，使用 MMRL、Magisk、KernelSU 或 APatch 安装，核心与管理器会一起安装。

- `WeiG-RootAd-test-core-only.zip`: core only / 仅核心。
- `WeiG-RootAd-Manager-test.apk`: standalone manager / 独立管理器。
